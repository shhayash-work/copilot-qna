# ============================================================
# Forge CLI Authentication
# ============================================================
# 
# Forge CLIは環境変数による認証をサポートしています。
# FORGE_EMAILとFORGE_API_TOKENを設定すれば、forge loginを実行する必要はありません。
#
# 【セットアップ手順】
#
# 1. このファイルを .env にコピー
#    cp .env.sample .env
#
# 2. .env を編集して実際の値を入力
#    nano .env
#
# 3. 環境変数を読み込んでForgeコマンドを実行
#    export $(grep -v '^#' .env | xargs) && npx forge whoami
#
# ============================================================
# 環境変数
# ============================================================

# Atlassianアカウントのメールアドレス
FORGE_EMAIL=your-email@example.com

# Atlassian APIトークン
# 取得方法: https://id.atlassian.com/manage/api-tokens
FORGE_API_TOKEN=your-api-token-here

# ============================================================
# 注意事項
# ============================================================
#
# - .envファイルは機密情報を含むため、Gitにコミットしないでください
# - .gitignoreに.envが追加されていることを確認してください
# - 共有環境では、各ユーザーが自分の.envファイルを作成してください
# - 環境変数は現在のシェルセッションでのみ有効です
# - 新しいターミナルを開いた場合は、再度exportコマンドを実行してください
#
# ============================================================
# 使用例
# ============================================================
#
# # 環境変数を読み込んでデプロイ
# export $(grep -v '^#' .env | xargs) && npm run deploy
#
# # 環境変数を読み込んでForge CLIを使用
# export $(grep -v '^#' .env | xargs) && npx forge whoami
# export $(grep -v '^#' .env | xargs) && npx forge install
